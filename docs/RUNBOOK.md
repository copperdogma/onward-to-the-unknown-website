# Runbook

This repo is still in bootstrap mode, so the operative commands are currently
the methodology and skill-sync surfaces.

## Bootstrap Commands

```bash
make skills-sync
make methodology-compile
make methodology-check
```

## Current Truth

- the imported skill surface lives in `.agents/skills/`
- `doc-web` is the upstream content-processing reference
- there is no chosen site runtime or deploy command yet

When the first real import and render paths land, extend this runbook with the
smallest honest command set.

